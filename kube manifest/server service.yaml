apiVersion: v1
kind: Service
metadata:
  name: server-service
spec:
  selector:
    app: tracing
    microservice: server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8082
  type: NodePort