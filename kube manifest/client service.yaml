apiVersion: v1
kind: Service
metadata:
  name: client-service
spec:
  selector:
    app: tracing
    microservice: client
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
  type: NodePort